name: workflow-services

on:
    push:
        branches:
            - master

jobs: 
  trabajo1: 
    runs-on: ubuntu-latest

    steps: 
      - name: listar archivos ahora
        run: ls -la
       